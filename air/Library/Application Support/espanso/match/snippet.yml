matches:
  - trigger: "/snip"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: cd /Volumes/swap/Sync/projects/snippets/snippets && ls **/*.* | choose | xargs cat

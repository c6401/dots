[alias]
	b = branch
	br = branch
	ci = commit
	cm = commit -m
	co = checkout
	cob = checkout -b
	ol = log --pretty=oneline  # oneline
	dh = "!f() { git diff HEAD~$1; }; f"  # diffhead
	fp = push --force-with-lease  # forcepush
	hr = "!f() { git reset --hard HEAD~$1; }; f"  # hardreset